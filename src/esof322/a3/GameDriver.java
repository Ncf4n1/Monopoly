package esof322.a3;

public class GameDriver {

	public static void main(String[] args) {
		// int numbPlayers = get input form user
		// roll for turn order
		// for each player
			// input name
			// select piece
			// construct: Player player = new Player (PlayerName, Piece)
			// Player [] Players = new Player [numbPlayers];
		//Create instance of Board (Not sure what the best way to go about this is :(, we will have to brainstorm)
		//for each player
			//Players[i] takes turn
				//turn stuff(this will be a big chunk of code).....
	}

	
}
